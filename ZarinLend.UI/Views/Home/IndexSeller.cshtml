@{
    ViewData["Title"] = ViewBag.Title;
    Layout = "~/Views/Shared/_NewLayout.cshtml";
}
@section styles
{
    <style type="text/css">
    </style>
}
@section scripts
{
    <script>

    </script>
}
<div class="row">
    <div class="col-12 mt-lg-0">
        <div class="rounded-lg bg-white p-1">
            <div class="d-flex align-items-center">
                <span class="w-50 text-bold-500 font-small-3">موجودی کیف پول</span><span class="w-50 text-bold-700 secondary darken-4 font-large-1">@await Component.InvokeAsync("UserWalletBalance") <label class="font-small-3 ml-75 text-bold-500">ريال</label></span>
            </div>
        </div>
    </div>
    <div class="col-12 mt-1">
        <div class="rounded-lg bg-white p-1 pt-2">
            <div class="d-flex align-items-center justify-content-between">
                <div class="font-medium-1 secondary darken-4 text-bold-700">
                    فاکتور های اخیر
                </div>
                <div>
                    <a href="@Url.Action("List","Invoice")" class="rounded dark border px-1 py-50">مشاهده همه</a>
                </div>
            </div>
            <div id="requestGridDiv" class="mt-2 ">
                @{
                    await Html.RenderPartialAsync("~/Views/Invoice/_SellerListPartial.cshtml", 5);
                }
            </div>
        </div>
    </div>
</div>
@{
    await Html.RenderPartialAsync("~/Views/Invoice/_UploadInvoiceDocumentPartial.cshtml", new Services.Model.Invoice.InvoiceImageUploadModel());
}