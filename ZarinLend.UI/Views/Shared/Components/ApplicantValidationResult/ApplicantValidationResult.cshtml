@model List<ApplicantValidationResultModel>

<style type="text/css">
</style>

<div class="bg-white p-2 rounded-lg">
    <div class="d-flex align-items-center justify-content-between">
        <div class="text-bold-700 dark font-medium-2">
            نتیجه استعلام های 8 گانه
        </div>
        @if (User.IsInRole(Core.Entities.RoleEnum.BankLeasing.ToString()))
        {
            <button id="btnDownloadOctetExcel" class="btn d-flex align-items-center border secondary darken-4 font-small-3 text-bold-500 justify-content-center">
                دانلود اکسل استعلام 8 گانه
                <i class="bx bx-file-blank ml-50"></i>
            </button>
        }
    </div>
    @if (Model != null && Model.Any())
    {
        var item = Model.Last();
        <div class="border rounded mt-2">
            <div class="d-flex flex-column flex-sm-row">
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">کاربر ثبت کننده</span>
                    <span class="dark mt-50 font-small-3 text-bold-400">@item.Creator</span>
                </div>
                <div class="border-left border-top"></div>
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">تاریخ ثبت استعلامات</span>
                    <span class="dark mt-50 font-small-3 text-bold-400">@item.ShamsiCreatedDate</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                @{
                    var caption = item.CivilRegistryInquiry.HasValue ? (item.CivilRegistryInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    var className = item.CivilRegistryInquiry.HasValue ? (item.CivilRegistryInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام ثبت احوال</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
                <div class="border-left border-top"></div>
                @{
                    caption = item.ReturnedCheckInquiry.HasValue ? (item.ReturnedCheckInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    className = item.ReturnedCheckInquiry.HasValue ? (item.ReturnedCheckInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام چک برگشتی</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                @{
                    caption = item.FacilityInquiry.HasValue ? (item.FacilityInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    className = item.FacilityInquiry.HasValue ? (item.FacilityInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام تسهیلات</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
                <div class="border-left border-top"></div>
                @{
                    caption = item.PostalCodeInquiry.HasValue ? (item.PostalCodeInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    className = item.PostalCodeInquiry.HasValue ? (item.PostalCodeInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام کد پستی</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                @{
                    caption = item.SecurityCouncilSanctionsInquiry.HasValue ? (item.SecurityCouncilSanctionsInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    className = item.SecurityCouncilSanctionsInquiry.HasValue ? (item.SecurityCouncilSanctionsInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام تحریم شورا امنیت</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
                <div class="border-left border-top"></div>
                @{
                    caption = item.ShahkarInquiry.HasValue ? (item.ShahkarInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    className = item.ShahkarInquiry.HasValue ? (item.ShahkarInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام شاهکار</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                @{
                    caption = item.MilitaryInquiry.HasValue ? (item.MilitaryInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    className = item.MilitaryInquiry.HasValue ? (item.MilitaryInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام نظام وظیفه</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
                <div class="border-left border-top"></div>
                 @{
                    caption = item.BlackListInquiry.HasValue ? (item.BlackListInquiry.Value ? "بله (تاییده شده)" : "خیر (رد شده)") : "-";
                    className = item.BlackListInquiry.HasValue ? (item.BlackListInquiry.Value ? "success " : "danger") : "dark";
                }
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام لیست سیاه</span>
                    <span class="@className darken-4 mt-50 font-small-3 text-bold-400">@caption</span>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="border rounded mt-2">
            <div class="d-flex flex-column flex-sm-row">
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">کاربر ثبت کننده</span>
                    <span class="dark mt-50 font-small-3 text-bold-400">-</span>
                </div>
                <div class="border-left border-top"></div>
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">تاریخ ثبت استعلامات</span>
                    <span class="dark mt-50 font-small-3 text-bold-400">-</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام ثبت احوال</span>
                    <span class="dark darken-4 mt-50 font-small-3 text-bold-400">-</span>
                </div>
                <div class="border-left border-top"></div>
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام چک برگشتی</span>
                    <span class="dark darken-4 mt-50 font-small-3 text-bold-400">-</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام تسهیلات</span>
                    <span class="dark darken-4 mt-50 font-small-3 text-bold-400">-</span>
                </div>
                <div class="border-left border-top"></div>
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام کد پستی</span>
                    <span class="dark darken-4 mt-50 font-small-3 text-bold-400">-</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام تحریم شورا امنیت</span>
                    <span class="dark darken-4 mt-50 font-small-3 text-bold-400">-</span>
                </div>
                <div class="border-left border-top"></div>
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام شاهکار</span>
                    <span class="dark mt-50 font-small-3 text-bold-400">-</span>
                </div>
            </div>
            <hr class="m-0 p-0" />
            <div class="d-flex flex-column flex-sm-row">
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام نظام وظیفه</span>
                    <span class="dark darken-4 mt-50 font-small-3 text-bold-400">-</span>
                </div>
                <div class="border-left border-top"></div>
                <div class="d-flex flex-column justify-content-start py-2 px-1 w-50">
                    <span class="font-small-2 text-bold-500">استعلام لیست سیاه</span>
                    <span class="dark darken-4 mt-50 font-small-3 text-bold-400">-</span>
                </div>
            </div>
        </div>
    }
</div>
@*<div class="card-body">
    @if (User.IsInRole(Core.Entities.RoleEnum.BankLeasing.ToString()))
    {
        <div class="row" style="margin-bottom:15px">
            <div class="col-md-6">
                <button id="btnDownloadOctetExcel" tabindex="5" class="btn btn-outline-info" >
                    @_loc["DownloadOctetExcel"]&nbsp;<i class="fal fa-file-excel-o"></i>
                </button>
            </div>
        </div>
    }
    @if (Model != null)
    {
        var columnCount = 11;
        <div class="paging-div"></div>
        <div class="table-responsive">
            <table class="table table-bordered table-hover table-custom">
                <thead>
                    <tr class="table-secondary">
                        <th scope="col">#</th>
                        @if (User.IsInRole(Core.Entities.RoleEnum.BankLeasing.ToString()))
                        {
                            columnCount += 1;
                            <th scope="col" data-sortorder="date" >نهاد مالی</th>
                        }
                        <th scope="col" data-sortorder="date" >استعلام ثبت احوال</th>
                        <th scope="col" data-sortorder="date" >استعلام چک برگشتی</th>
                        <th scope="col" data-sortorder="date" >استعلام تسهیلات</th>
                        <th scope="col" data-sortorder="date" >استعلام کد پستی</th>
                        <th scope="col" data-sortorder="date" >استعلام تحریم شورا امنیت</th>
                        <th scope="col" data-sortorder="date" >استعلام شاهکار</th>
                        <th scope="col" data-sortorder="date" >استعلام نظام وظیفه</th>
                        <th scope="col" data-sortorder="date" >استعلام لیست سیاه</th>
                        <th scope="col" data-sortorder="date" >کاربر ثبت کننده</th>
                        <th scope="col" data-sortorder="Tedad" >تاریخ ثبت استعلامات</th>
                    </tr>
                </thead>
                <tbody id="requestGridRows">
                    @if (Model.Any())
                    {
                        for (int i = 0; i < Model.Count; i++)
                        {
                            <tr class='@(i % 2 == 0 ? "table-info" : "table-light")'>
                                <td>@(i + 1)</td>
                                @if (User.IsInRole(Core.Entities.RoleEnum.BankLeasing.ToString()))
                                {
                                    <td>@Model[i].OrganizationName</td>
                                }
                                <td>
                                    @Html.Raw(Model[i].CivilRegistryInquiry.HasValue
                        ? (Model[i].CivilRegistryInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:red'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>
                                    @Html.Raw(Model[i].ReturnedCheckInquiry.HasValue
                        ? (Model[i].ReturnedCheckInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:red'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>
                                    @Html.Raw(Model[i].FacilityInquiry.HasValue
                        ? (Model[i].FacilityInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:red'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>
                                    @Html.Raw(Model[i].PostalCodeInquiry.HasValue
                        ? (Model[i].PostalCodeInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:red'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>
                                    @Html.Raw(Model[i].SecurityCouncilSanctionsInquiry.HasValue
                        ? (Model[i].SecurityCouncilSanctionsInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:grereden'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>
                                    @Html.Raw(Model[i].ShahkarInquiry.HasValue
                        ? (Model[i].ShahkarInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:red'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>
                                    @Html.Raw(Model[i].MilitaryInquiry.HasValue
                        ? (Model[i].MilitaryInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:red'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>
                                    @Html.Raw(Model[i].BlackListInquiry.HasValue
                        ? (Model[i].BlackListInquiry.Value ? "<span style='color:green'>بله(تایید شده)</span>" : "<span style='color:red'>خیر(رد شده)</span>")
                        : "نامشخص")
                                </td>
                                <td>@Model[i].Creator</td>
                                <td>@Model[i].ShamsiCreatedDate</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="@columnCount" >
                                نتیجه استعلام ثبت نشده است
                            </td>
                        </tr>
                    }
                </tbody>
                <tfoot>
                </tfoot>
            </table>
        </div>
        <div class="paging-div"></div>
    }
</div>*@
<script on-content-loaded="true">
</script>
